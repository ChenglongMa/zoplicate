name: Zotero Upstream Watch

on:
  schedule:
    - cron: "17 3 * * *"
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: zotero-upstream-watch
  cancel-in-progress: false

jobs:
  watch:
    runs-on: ubuntu-latest
    env:
      GITHUB_TOKEN: ${{ github.token }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install uv
        run: python -m pip install --upgrade uv

      - name: Check Zotero upstream contracts
        run: uv run python .claude-workflow/scripts/ci/check_zotero_upstream.py --update

      - name: Validate workflow metadata
        run: |
          uv run python -m unittest discover .claude-workflow/scripts -p 'test_*.py'
          uv run python .claude-workflow/scripts/ci/check_stop.py

      - name: Create upstream watch PR
        uses: peter-evans/create-pull-request@v8
        with:
          token: ${{ github.token }}
          branch: chore/zotero-upstream-watch
          delete-branch: true
          title: "chore: update Zotero upstream watch"
          commit-message: "chore: update Zotero upstream watch"
          body-path: .claude-workflow/docs/ai/upstream/zotero_upstream_report.md
          labels: |
            upstream-zotero
            maintenance
          add-paths: |
            .claude-workflow/docs/ai/upstream/zotero_watch_targets.json
            .claude-workflow/docs/ai/upstream/zotero_upstream_contract.json
            .claude-workflow/docs/ai/upstream/zotero_upstream_report.md
            .claude-workflow/docs/ai/milestones/*.json
            .claude-workflow/docs/ai/milestone_index.json
            .claude-workflow/docs/ai/project_snapshot.json
